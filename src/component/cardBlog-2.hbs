{{#with data}}
    <main class="card">
        <div class="card-image" style="background-image: url({{photo}})"></div>
        <h2 class="card-1">{{heading}}</h2>
        <p class="card-2">{{info}}</p>
        <a href="{{linkUrl}}" class="card-3">{{linkText}}</a>
    </main>
{{/with}}