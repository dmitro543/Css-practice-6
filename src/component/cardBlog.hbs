{{#with data}}
    <main class="card">
        <div class="card-image" style="background-image: url({{img}})"></div>
        <h2 class="card-1">{{title}}</h2>
        <p class="card-2">{{text}}</p>
        <a href="{{href}}" class="card-3">{{button}}</a>
    </main>
{{/with}}